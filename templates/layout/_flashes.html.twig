{% for type, messages in app.flashes() %}

    {% for message in messages %}
        <twig:Toast type="{{ type }}" floating autoclose="5000">
            {{ message }}
        </twig:Toast>
    {% endfor %}

{% endfor %}